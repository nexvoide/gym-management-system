import type { LucideIcon } from "lucide-react";
export function PhasePlaceholder({ title, description, icon:Icon, phase }:{title:string;description:string;icon:LucideIcon;phase:number}) { return <div className="content"><div className="page-head"><div><div className="eyebrow">Phase {phase}</div><h1>{title}</h1><p>{description}</p></div><span className="badge">Foundation ready</span></div><div className="card placeholder"><div><div className="placeholder-icon"><Icon size={23}/></div><h2>{title} is intentionally staged</h2><p>The secure route, navigation, data ownership, and permission boundary are ready. The complete workflow belongs to Phase {phase}, after the foundation is verified.</p></div></div></div> }
