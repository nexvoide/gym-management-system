"use client";import { useActionState } from "react";import { Plus } from "lucide-react";import { createCategory } from "@/app/(app)/expenses/actions";export function ExpenseCategoryForm(){const [state,action,pending]=useActionState(createCategory,{});return <form action={action} className="category-form"><input className="input" name="name" placeholder="New category name" required/><button className="btn btn-primary" disabled={pending}><Plus size={15}/>{pending?"Adding…":"Add category"}</button>{state.error&&<span className="field-error">{state.error}</span>}</form>}
