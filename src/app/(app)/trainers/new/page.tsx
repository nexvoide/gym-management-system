import { requirePermission } from "@/lib/auth";import { TrainerForm } from "@/components/trainer-form";import { createTrainer } from "../actions";export default async function Page(){await requirePermission("trainers.write");return <div className="content content-narrow"><div className="page-head"><div><div className="eyebrow">Coaching team</div><h1>Add trainer</h1><p>Create a focused profile without scheduling overhead.</p></div></div><TrainerForm action={createTrainer}/></div>}
