import { eq } from "drizzle-orm";
import { db } from "@/db";
import { gyms } from "@/db/schema";
import { requirePermission } from "@/lib/auth";
import { updateGymProfile } from "./actions";
import Link from "next/link";
import { can } from "@/lib/permissions";
export default async function SettingsPage() { const user = await requirePermission("settings.read"); const gym = (await (db.select().from(gyms)).where(eq(gyms.id, user.gymId)))[0]!; return <div className="content"><div className="page-head"><div><div className="eyebrow">Workspace configuration</div><h1>Settings</h1><p>Keep your gym’s defaults clear and consistent.</p></div></div><div className="settings-grid"><aside className="card settings-menu"><span className="settings-link active">Gym profile</span>{can(user.role, "users.manage") && <Link className="settings-link" href="/settings/staff">Staff & roles</Link>}</aside><form className="card form-card" action={updateGymProfile}><h3>Gym profile</h3><p>The identity and regional defaults used across the workspace.</p><div className="form-grid"><div className="field span-2"><label htmlFor="name">Gym name</label><input className="input" id="name" name="name" defaultValue={gym.name} required/></div><div className="field"><label htmlFor="email">Email</label><input className="input" id="email" name="email" type="email" defaultValue={gym.email ?? ""}/></div><div className="field"><label htmlFor="phone">Phone</label><input className="input" id="phone" name="phone" defaultValue={gym.phone ?? ""}/></div><div className="field span-2"><label htmlFor="address">Address</label><input className="input" id="address" name="address" defaultValue={gym.address ?? ""}/></div><div className="field"><label htmlFor="timezone">Timezone</label><select className="input" id="timezone" name="timezone" defaultValue={gym.timezone}><option>Asia/Karachi</option><option>UTC</option><option>Europe/London</option><option>America/New_York</option><option>Asia/Dubai</option></select></div><div className="field"><label htmlFor="currency">Currency</label><select className="input" id="currency" name="currency" defaultValue={gym.currency}><option value="PKR">PKR — Pakistani Rupee</option><option value="USD">USD — US Dollar</option><option value="GBP">GBP — British Pound</option><option value="AED">AED — UAE Dirham</option><option value="EUR">EUR — Euro</option></select></div><div className="field"><label htmlFor="locale">Language & format</label><select className="input" id="locale" name="locale" defaultValue={gym.locale}><option value="en-PK">English (Pakistan)</option><option value="en-US">English (United States)</option><option value="en-GB">English (United Kingdom)</option></select></div></div><div className="save-row"><button className="btn btn-primary" disabled={!(["owner", "manager"] as string[]).includes(user.role)}>Save changes</button></div></form></div></div>; }
