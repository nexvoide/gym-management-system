import {PasswordForm} from "./password-form";export default async function Page({searchParams}:{searchParams:Promise<{token?:string;recovery?:string}>}){const {token,recovery}=await searchParams;const isRecovery=recovery==="1";return <main className="auth-page"><section className="auth-story"><div className="brand"><span className="brand-mark">F</span><span>Form</span></div><div className="story-copy"><div className="story-kicker">Secure staff access</div><h1>Choose your<br/>own password.</h1><p>This one-time setup keeps permanent credentials private.</p></div></section><section className="auth-panel"><div className="auth-card"><h2>{isRecovery?"Reset password":"Finish account setup"}</h2><p className="muted">Use at least 12 characters including upper and lowercase letters, a number, and a symbol.</p>{token?<PasswordForm token={token}/>:isRecovery?<PasswordForm recovery/>:<div className="error">This setup link is incomplete.</div>}</div></section></main>}
